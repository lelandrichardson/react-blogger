var React = require('react');

class Loading extends React.Component {
    render() {
        return (
            <div>
                Loading...
            </div>
        );
    }
}

module.exports = Loading;